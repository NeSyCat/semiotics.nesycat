# 04-prototype

Interactive prototype — HTML+CSS+JS. Click-throughs, hover states, basic flows.

This is the last static phase before code is translated into React under `/app/` and `/components/`.

Open `index.html` in a browser to view the current prototype.
